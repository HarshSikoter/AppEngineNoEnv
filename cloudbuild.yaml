# steps:
#   # Step 1: Install dependencies
#   - name: 'gcr.io/cloud-builders/npm'
#     args: ['install']

#   # Step 2: Deploy to Google App Engine
#   - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
#     args: ['gcloud', 'app', 'deploy']

# options:
#   logging: CLOUD_LOGGING_ONLY  # Ensures logs are sent to Cloud Logging
#   # logsBucket: gs://your-logs-bucket-name  # Replace with your actual Cloud Storage bucket name

# # Timeout for the build process (optional, default is 10 minutes)
# # timeout: '900s'

# steps:
#   # Step 1: Install dependencies
#   - name: 'gcr.io/cloud-builders/npm'
#     args: ['install']
#     dir: 'ejs-app'  # <-- your actual folder

#   # Step 2: Deploy to Google App Engine
#   - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
#     args: ['gcloud', 'app', 'deploy', 'app.yaml']
#     dir: 'ejs-app'  # <-- same folder

# options:
#   logging: CLOUD_LOGGING_ONLY


steps:
  # Step 1: Install dependencies
  - name: 'gcr.io/cloud-builders/npm'
    args: ['install']
    dir: 'ejs-app'  # Ensure npm is run in the ejs-app directory

  # Step 2: Deploy to Google App Engine
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    args: ['gcloud', 'app', 'deploy', 'ejs-app/app.yaml']
    dir: '.'  # Run the deployment from the root directory

options:
  logging: CLOUD_LOGGING_ONLY  # Ensures logs are sent to Cloud Logging
